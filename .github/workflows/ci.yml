name: CI

on:
  pull_request:
    branches:
      - main
      - develop
  push:
    branches:
      - main
      - develop
      - "feature/**"
      - "release/**"

jobs:
  npm-checks:
    uses: ./.github/workflows/reusable-node-ci.yml
    with:
      node-version: "20"
      run-script: ci
